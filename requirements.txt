# Compatibility notice only.
#
# requirements.txt is no longer an installation source. Direct dependencies
# are declared in pyproject.toml and the complete reproducible environment is
# pinned in uv.lock.
#
# Production:
#   uv sync --locked --no-dev --python 3.12
#
# Development:
#   uv sync --locked --python 3.12
